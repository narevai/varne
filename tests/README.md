# Tests

To re-record all the cassettets, run:

```bash
pytest --record-mode=once
```

For a single test:

```bash
pytest /workspace/tests/unit/providers/jsonplaceholder/test_service.py --record-mode=once
```
